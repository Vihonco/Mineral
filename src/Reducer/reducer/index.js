const initialState={}
function rootreducer(state = initialState, action) {
    
    switch (action.type) {
        // Maneja aquí las acciones
        default:
            return state;
    }
}


export default rootreducer;